Loops, Array and Conditions

Arrays = conjunto de valores

Syntax FOR:
For ( < type > counter = n; < boolean_expression>; <counter_increment> ) {
                code_block;
          }

Example:
            for ( int i = 1; i < 5; i ++) {
                    sout("i is " +i +"");

            }

